import { createStore } from 'vuex'

const state = {}
const mutations = {}
const actions = {}
const modules = {}

const store = createStore({
    namespaced: true,
    state,
    mutations,
    actions,
    modules
})

export default store
